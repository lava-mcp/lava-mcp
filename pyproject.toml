[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "lava-mcp"
version = "0.1.0"
description = "MCP server exposing a LAVA instance to agents"
readme = "README.md"
requires-python = ">=3.11"
license = "MIT"
authors = [{ name = "Matt Hart" }]
dependencies = [
    # Cap below 2.0: mcp 2.x drops the `mcp.server.fastmcp` import path this server
    # uses (a fresh unpinned build pulled 2.0 and failed at import). Stay on the 1.x
    # FastMCP until we migrate.
    "mcp>=1.0,<2",
    "requests>=2.28",
    "PyYAML>=6.0",
    "asyncssh>=2.14",
    # ASGI-server WebSocket protocol implementation. Starlette provides the WS route
    # abstraction, but uvicorn needs this (or wsproto) to perform the /mcp/gateway-ssh
    # upgrade handshake; without it uvicorn rejects the upgrade and returns 404.
    "websockets>=13",
]

[project.optional-dependencies]
dev = [
    "pytest>=7",
    "responses>=0.23",
    "black",
    "isort",
    "mypy",
    "types-requests",
    "types-PyYAML",
]

[project.scripts]
lava-mcp = "lava_mcp.cmdline:main"

[tool.setuptools.packages.find]
include = ["lava_mcp*"]

[tool.black]
target-version = ["py311"]

[tool.isort]
profile = "black"

[tool.pytest.ini_options]
testpaths = ["tests"]
